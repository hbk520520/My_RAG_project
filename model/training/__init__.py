"""
训练脚本子包。

使用方式：
  python -m model.training.train_meta_planner
  python -m model.training.train_replanner_grpo
  python -m model.training.train_extractor_grader
  python -m model.training.train_reasoner
  python -m model.training.train_retriever

数据生成：
  python -m model.data.evol_instruct
"""
